import 'package:flutter/material.dart';

class CameraFragment extends StatelessWidget {
  const CameraFragment({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
